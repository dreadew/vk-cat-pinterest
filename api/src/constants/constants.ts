const envFilePath = '.env';

const dbHostEnv = 'DB_HOST';
const dbNameEnv = 'DB_NAME';
const dbPortEnv = 'DB_PORT';
const dbUserEnv = 'DB_USER';
const dbPasswordEnv = 'DB_PASSWORD';
const dbSslEnv = 'DB_SSL';

const jwtSecretEnv = 'JWT_SECRET';
const jwtExpiresEnv = 'JWT_EXPIRES';

export {
  dbHostEnv,
  dbNameEnv,
  dbPasswordEnv,
  dbPortEnv,
  dbSslEnv,
  dbUserEnv,
  envFilePath,
  jwtExpiresEnv,
  jwtSecretEnv,
};
